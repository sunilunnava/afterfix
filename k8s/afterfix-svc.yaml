{
    "kind": "Service",
    "apiVersion": "v1",
    "metadata": {
        "name": "afterfix",
         "namespace": "com-att-ocnp-mgmt"
    },
    "spec": {
        "selector": {
            "app": "afterfix"
        },
        "ports": [
            {
                "protocol": "TCP",
                "port": 80,
                "targetPort": 8080
            }
        ],
        "type": "NodePort"
    }
}
